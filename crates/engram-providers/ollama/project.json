{
  "name": "engram-provider-ollama",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build -p engram-provider-ollama",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo test -p engram-provider-ollama",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo clippy -p engram-provider-ollama -- -D warnings",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
