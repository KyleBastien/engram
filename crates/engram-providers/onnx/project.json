{
  "name": "engram-provider-onnx",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build -p engram-provider-onnx",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo test -p engram-provider-onnx",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo clippy -p engram-provider-onnx -- -D warnings",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
