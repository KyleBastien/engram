[package]
name = "engram-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
engram-core = { path = "../engram-core" }
engram-store = { path = "../engram-store" }

[dev-dependencies]
tempfile = "3"
